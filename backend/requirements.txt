# Note: backend/ also imports from mcp_server/ and storage/ via sys.path
# These are in the parent directory (repo root)
fastapi>=0.110.0
uvicorn>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.0
anthropic>=0.40.0
mcp>=1.0.0
pydantic-settings>=2.0.0
slowapi>=0.1.9
stripe>=8.0.0
